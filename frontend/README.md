# Frontend Placeholder

The frontend will live here later. Planned stack: React/Vite/TypeScript with API
calls into `src/api`.

Initial pages:

- `/reviews/new`
- `/reviews/:run_id`
- `/history`
- `/venues`
- `/settings`
