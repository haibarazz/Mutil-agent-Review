# Reference Material

`legacy-coze-review/` contains the archived Coze/Vibe implementation that was
present before the local-first rewrite.

Use it for workflow, prompt, venue-profile, and UI reference. Active development
belongs in the root-level `src/` package.
