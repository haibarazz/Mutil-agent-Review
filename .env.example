APP_ENV=dev
DATA_DIR=./data
REVIEW_VERBOSE=false
REVIEW_LLM_VERBOSE=false
APP_CORS_ORIGINS=http://127.0.0.1:5173,http://localhost:5173
APP_SUPPORTED_UPLOAD_EXTENSIONS=.pdf,.md,.tex
APP_MAX_UPLOAD_BYTES=83886080

# mock keeps local smoke runs independent from external services.
# Options:
# - mock: local deterministic responses, no external API calls.
# - openai_compatible: one global OpenAI-compatible provider.
# - router: route each prompt model id through configs/llm.yaml.
LLM_PROVIDER=mock
# mock 演示模式下，每个 LangGraph 节点额外停留几秒，方便观察前端审稿剧场。
MOCK_NODE_DELAY_SEC=0
LLM_BASE_URL=
LLM_API_KEY=
LLM_DEFAULT_MODEL=gpt-4o-mini
LLM_TIMEOUT_SEC=600
LLM_CONFIG_PATH=configs/llm.yaml

# Router provider credentials. Fill only the providers you actually use.
LLM_SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1
LLM_SILICONFLOW_API_KEY=
LLM_DEEPSEEK_BASE_URL=https://api.deepseek.com
LLM_DEEPSEEK_API_KEY=
# OpenRouter credentials. The OPENOUTER name is kept to match the local .env.
LLM_OPENOUTER_BASE_URL=https://openrouter.ai/api/v1
LLM_OPENOUTER_API_KEY=

SEARCH_PROVIDER=none
FETCH_PROVIDER=none
# auto: use MinerU first when MINERU_API_TOKEN is configured, then fallback to PyMuPDF.
# pymupdf/local: skip MinerU and use local PyMuPDF for PDF parsing.
PARSER_BACKEND=auto

MINERU_API_TOKEN=
MINERU_BASE_URL=https://mineru.net
MINERU_MODEL_VERSION=vlm
MINERU_TIMEOUT_SEC=300
MINERU_POLL_INTERVAL_SEC=3
MINERU_REQUEST_TIMEOUT_SEC=30

LEGACY_REFERENCE_DIR=reference/legacy-coze-review
PROMPTS_DIR=prompts
VENUES_DIR=venues
